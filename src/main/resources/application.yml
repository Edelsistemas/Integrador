spring:
  main:
    allow-bean-definition-overriding: true
  batch:
    job:
      enabled: false
sap:
  edelflex:
    url: https://10.250.2.10:50000/b1s/v1/
    credentials:
      user: gregorio.hakimian
      password: 1234
      company: TEST_EDELFLEX

sqlserver:
  jdbcUrl: jdbc:sqlserver://localhost:1433;encrypt=true;trustServerCertificate=true;databaseName=master
  username: sa
  password: LongPassword123
  driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver

team-center:
  querys:
    schema: DB_Edelflex_SAP
    acce-inline:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.AcceInline
      update: UPDATE FROM ${team-center.querys.schema}.dbo.AcceInline
    bom-des-pos:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.BomDesPos
      update: UPDATE FROM ${team-center.querys.schema}.dbo.BomDesPos
    bomba-cen:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.BombaCen
      update: UPDATE FROM ${team-center.querys.schema}.dbo.BombaCen
    disp-limpi:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.DispLimpi
      update: UPDATE FROM ${team-center.querys.schema}.dbo.DispLimpi
    homogen:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Homogen
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Homogen
    instr-ind:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.InstrInd
      update: UPDATE FROM ${team-center.querys.schema}.dbo.InstrInd
    instrumen:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Instrumen
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Instrumen
    inter-calor:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Inter_Calor
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Inter_Calor
    mat-pri:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.MatPri
      update: UPDATE FROM ${team-center.querys.schema}.dbo.MatPri
    otras-bom:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.OtrasBom
      update: UPDATE FROM ${team-center.querys.schema}.dbo.OtrasBom
    otras-val:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.OtrasVal
      update: UPDATE FROM ${team-center.querys.schema}.dbo.OtrasVal
    otros-comp:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.OtrosComp
      update: UPDATE FROM ${team-center.querys.schema}.dbo.OtrosComp
    pieza:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Pieza
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Pieza
    repuestos:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Repuestos
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Repuestos
    resto-inter:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.RestoInter
      update: UPDATE FROM ${team-center.querys.schema}.dbo.RestoInter
    semielabo:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Semielabo
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Semielabo
    sis-cal-eh:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.SisCalEH
      update: UPDATE FROM ${team-center.querys.schema}.dbo.SisCalEH
    sis-lim-ec:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.SisLimEC
      update: UPDATE FROM ${team-center.querys.schema}.dbo.SisLimEC
    sis-mez-em:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.SisMezEM
      update: UPDATE FROM ${team-center.querys.schema}.dbo.SisMezEM
    sis-rec-prod:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.SisRecProd
      update: UPDATE FROM ${team-center.querys.schema}.dbo.SisRecProd
    sistemas:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Sistemas
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Sistemas
    subcon:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Subcon
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Subcon
    tanques:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Tanques
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Tanques
    val-ali-seg:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.ValAliSeg
      update: UPDATE FROM ${team-center.querys.schema}.dbo.ValAliSeg
    val-asiento:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.ValAsiento
      update: UPDATE FROM ${team-center.querys.schema}.dbo.ValAsiento
    val-control:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.ValControl
      update: UPDATE FROM ${team-center.querys.schema}.dbo.ValControl
    val-dia:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.ValDia
      update: UPDATE FROM ${team-center.querys.schema}.dbo.ValDia
    val-fuelle:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.ValFuelle
      update: UPDATE FROM ${team-center.querys.schema}.dbo.ValFuelle
    val-reten:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.ValReten
      update: UPDATE FROM ${team-center.querys.schema}.dbo.ValReten
    valvulas:
      get: SELECT * FROM ${team-center.querys.schema}.dbo.Valvulas
      update: UPDATE FROM ${team-center.querys.schema}.dbo.Valvulas


batch-process:
  sync-business-partners: 1800000

bitone:
  integration:
    api-key: "d896c408-9f6c-45b7-8ef1-59c323dd67f8"
    url: "http://localhost:8095/services/api/external-batch-process/register"